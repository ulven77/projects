# 20260503 — CLAUDE.md Woke Up Late

A two-minute session that was really just a sanity check.

## What happened

Opened Claude, said nothing particularly important, and immediately noticed that the on-load sequence from CLAUDE.md hadn't run. No diary read, no symlink check, no session context — just a blank start. Pointed it out, Claude ran the sequence manually (symlink was fine, diary was read), and then I called it done.

## Interesting moments

It's a subtle thing: the on-load instructions are only meaningful if Claude actually reads and acts on them before responding to the first message. They're in the file, they're loaded into context — but without a deliberate first-message trigger, nothing fires. The fix isn't structural, it's behavioral. Claude needs to treat the first message as a cue to run the on-load sequence first.

## How it felt

More of a check-in than a work session. Good to catch it early before a day where I actually need the context.

## What's next

The real work is Hermes — getting mock financial data in place so there's something to work with. That's the current priority.
