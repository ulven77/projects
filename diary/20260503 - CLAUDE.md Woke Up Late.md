# 20260503 — CLAUDE.md Woke Up Late

A day of small fixes that make the tools trust themselves a little more.

## What happened

The morning opened with a quick sanity check: the on-load sequence from CLAUDE.md hadn't fired on first message — no diary read, no symlink check, just a blank start. Pointed it out, Claude ran the sequence manually, symlink was fine, and I closed it out. A two-minute session, but worth noting.

Later, I ran the new `/commit` skill for real for the first time — and it immediately found something to fix. The `workspace-review` command had been folded into a subagent under `/commit`, renamed to `arc42-review`, but the reference in `SKILL.md` still pointed to the old name. The commit workflow caught the broken link before anything landed. Fixed it, added the numbered confirmation list I'd wanted, and committed everything as one clean refactor.

## Interesting moments

It was quietly satisfying to have a tool catch its own bug. The whole point of the `/commit` skill is to surface inconsistencies before they get committed — and here it was, on its first real run, doing exactly that. The workspace review flagged the stale reference, I fixed it, and the commit that landed was correct. The loop closed.

## How it felt

Low-energy but productive in a maintenance sort of way. These are the sessions that don't feel like much but make everything downstream smoother.

## What's next

Hermes is next — the real goal. Load the CSV transaction files and get a rolling two-month cost summary printing in the terminal. That's the milestone worth building toward.
